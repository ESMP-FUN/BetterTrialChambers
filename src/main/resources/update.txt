<black>.
<aqua>[1.5.14] <gray>- <white>2026-06-19
<lime>+ <white>FIX: <green>Right-clicking a vault no longer sends <yellow>duplicate<green> messages (e.g. the "need a Trial Key" line shown twice). Vault and key behaviour is unchanged.
<aqua>[1.5.13] <gray>- <white>2026-06-19
<lime>+ <white>FIX: <green>MySQL/MariaDB servers couldn't save vaults (<yellow>error in your SQL syntax … near 'CONFLICT'<green>). Vault saving and per-player open tracking now use the correct upsert on MySQL/MariaDB. SQLite is unaffected.
<lime>+ <white>FIX: <green><yellow>/tcp give<green> and <yellow>/tcp stats<green> against an unknown player no longer show a raw <yellow>Message not found<green> line.
<aqua>[1.5.12] <gray>- <white>2026-06-17
<lime>+ <white>NEW: <green>Vault economy rewards in loot tables — pay money on vault open via an <yellow>economy-rewards<green> list (weight + amount/min-max). Works with EssentialsX, CMI, any Vault economy; skipped cleanly if none is installed.
<lime>+ <white>FIX: <green>Time-based vault cooldowns now actually work — <yellow>vaults.normal/ominous-cooldown-hours > 0<green> reopens the vault that many hours after opening (with a remaining-time message). <yellow>0<green> = permanent until reset, as before.
<lime>+ <white>FIX: <green>Chamber completions are now recorded — the chambers leaderboard, <yellow>/tcp stats<green>, and the <yellow>%tcp_chambers_completed%<green> / <yellow>%tcp_top_chambers_*%<green> placeholders were stuck at 0. Credited per participant on a full chamber clear (toggle: <yellow>statistics.track-chamber-completion<green>).
<black>.
