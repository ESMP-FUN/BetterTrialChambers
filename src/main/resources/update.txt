<black>.
<aqua>[2.0.8] <gray>- <white>2026-07-21
<lime>+ <white>FIX: <green>2.0.7 could not be loaded by recent Paper builds at all — the plugin silently never appeared in <yellow>/plugins<green>, with nothing in the console to explain why. If 2.0.7 seemed to do nothing on your server, this is the fix.
<lime>+ <white>FIX: <green>MySQL is now actually included. <yellow>config.yml<green> has offered it since the first release, but the part that talks to MySQL was never shipped — it only worked if your server software happened to provide its own copy.
<lime>+ <white>NEW: <green>Choose who gets the loot from a vault with <yellow>vaults.loot-mode<green>: everyone their own copy (as before), first come first served (one reward per vault for the whole server), or leave vaults to Minecraft.
<lime>+ <white>FIX: <green>Testing vaults as an admin no longer quietly locks you out of them — a bypassing player now leaves no trace on the vault. New <yellow>/trial vault unlockall <chamber|all><green> opens every vault up again without waiting for a reset.
<lime>+ <white>NEW: <green><yellow>target-cooldown-length<green> in spawner_presets.yml is now optional — leave it out and the preset follows your server-wide spawner rest time. Set <yellow>reset.spawner-cooldown-overrides-presets<green> to force every spawner onto one time.
<aqua>[2.0.7] <gray>- <white>2026-07-19
<lime>+ <white>NEW: <green>Anonymous usage metrics moved from bStats to FastStats — the same handful of aggregate numbers, no player data. Switch it off with <yellow>metrics.enabled: false<green>. Nothing is sent on the first run, so you get a chance to opt out first.
<lime>+ <white>NEW: <green>Optional error reporting, off unless you turn it on. <yellow>metrics.error-reporting: true<green> reports only this plugin's own errors, with IP addresses, file paths, database credentials and player UUIDs stripped out before anything leaves your server.
<aqua>[2.0.4] <gray>- <white>2026-07-19
<lime>+ <white>FIX: <green>"The chamber has been reset!" no longer goes to the whole server — it now goes to the players who were actually inside that chamber. Set <yellow>global.reset-complete-audience: server<green> for the old behaviour.
<lime>+ <white>NEW: <green>Optional permission for the reset message — set <yellow>global.reset-complete-permission<green> and only players who have it are told.
<lime>+ <white>FIX: <green>Pointing a chamber at another loot table now actually works — its Normal/Ominous Loot buttons follow the override instead of editing a table nothing was using.
